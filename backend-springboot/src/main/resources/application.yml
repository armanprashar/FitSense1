spring:
  datasource:
    url: ${DATABASE_URL}
    username: ${PGUSER}
    password: ${PGPASSWORD}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

app:
  jwtSecret: ${JWT_SECRET}
  jwtExpirationMs: 86400000
  aiBaseUrl: ${AI_BASE_URL:http://localhost:8000}